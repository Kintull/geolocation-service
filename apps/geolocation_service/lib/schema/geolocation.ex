defmodule GeolocationService.Schemas.Geolocation do
  use Ecto.Schema

  schema "geolocation" do
    field :ip_address, :binary
    field :ip_address, :binary
    field :ip_address, :binary

  end
end
